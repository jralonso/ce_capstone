# Dynamic inentory
plugin: aws_ec2
boto_profile: jra-udacity
# Populate inventory with instances in these regions
regions:
  - "{{ lookup('env','AWS_REGION') }}"
filters:
  # All instances with their `Environment` tag set to SERVER_ENV defined in Jenkinsfile
  tag:Environment: {{ lookup('env','SERVER_ENV') }}